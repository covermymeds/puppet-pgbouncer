;; Autogenerated by puppet module
;; Do not manually update

[databases]
<% @databases.each do |entry| %>
<%= entry -%>
<% end %>

[pgbouncer]

;; Set hieradata hash value for "pgbouncer::params::config_params" to override
;; below values
<% @load_config_params.each_pair do |name, param| -%>
<%= "#{name} = #{param}" %>
<% end -%>
